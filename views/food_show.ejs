<!DOCTYPE html>
<html lang="en">
    <%- include("partials/head.ejs") %> 
    <body class="food-ejs">
        <a href="/menu"><h1>At food bar</h1></a>
        <div class="img-container zoom"> 
            <div><%= foodItem.name %>, $<%= foodItem.price %></div>
            <img src="<%= foodItem.image %> " alt="<%= foodItem.name %>">
        </div>
    </body>
</html>